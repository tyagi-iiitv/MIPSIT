# While working on ubuntu using wine

# If the library MFC42.DLL isn't found then install it using the following commands
	# sudo add-apt-repository  ppa:ubuntu-wine/ppa
	# sudo apt-get update && sudo apt-get updrade
	# winetrics mfc42
